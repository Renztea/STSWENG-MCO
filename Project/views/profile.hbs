<!DOCTYPE html>
<html>

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width,initial-scale=1.0">
        <title>Profile</title> 
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css" integrity="sha512-KfkfwYDsLkIlwQp6LFnl8zNdLGxu9YAA1QvwINks4PhcElQSvqcyVLLD9aMhXd13uQjoXtEKNosOWaZqXgel0g==" crossorigin="anonymous" referrerpolicy="no-referrer" />
        <link rel="stylesheet" href="../css/style.css">
        <link href='https://fonts.googleapis.com/css?family=Roboto' rel='stylesheet'>
        <link href='https://fonts.googleapis.com/css?family=Poppins' rel='stylesheet'>
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
        <script type="text/javascript" src="/js/index.js"></script>
    </head>
    
    <body>
        {{#if loggedin}}
            {{> loggedinheader}}
        {{else}}
            {{> notloggedinheader}}
        {{/if}}
        <div class="profilepage">
            <div class="profileinfo">
                <form action="/editprofile" method="GET">
                    <img src="{{ profile.avatar }}" alt="Avatar" class="avatar" id="profileimg">
                    </br>
                    <div><span id="profilename">{{ profile.username }}</span></div>
                    <br>
                    <div><span id="profiledate">{{ profile.birthday }}</span></div>
                    <br>
                    <div><span id="profilegender">{{ profile.gender }}</span></div>
                    <br>
                    <div><span id="profileemail">{{ profile.email }}</span></div>
                    <br>
                    <div><span id="profilecontact">{{ profile.phone }}</span></div>
                    <br>
                    <div><span id="profiledesc">{{ profile.description }}</span></div>
                    <button>Edit Profile</button>
                </form>
            </div>
            <div class="background">
                <div class="container" id="userBlog">
                    {{#each preview}}
                        {{> previews}}
                    {{/each}}
                </div>
            </div>
        </div>
    </body>

</html>